interface constructorOptions {
    symbols?: boolean;
    text?: boolean;
    date?: boolean;
};

interface printOptions {
    symbol: string;
    txt: string;
    color: string;
};

export {
    constructorOptions,
    printOptions
};